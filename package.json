{
  "scripts": {
    "build": "node build.mjs",
    "test": "node test.js"
  },
  "devDependencies": {
    "@aws-sdk/client-ssm": "^3.362.0",
    "minify": "^9.1.0"
  },
  "engines": {
    "node": "^20"
  }
}
